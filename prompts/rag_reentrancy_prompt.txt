【重入保护机制识别规则】
- 如果函数使用了 nonReentrant / noReentrant 修饰符，说明已受重入锁保护，不应判定为漏洞。
- nonReentrant 工作原理：执行函数体前 locked=true 上锁，require(!locked) 阻止重入，执行后 locked=false 解锁。
- 合约继承 ReentrancyGuard + 函数带 nonReentrant = safe（fixed）。
- 但是上下文的安全摘要中会明确指出 nonReentrant 保护是否存在绕过风险，请结合摘要综合判断。

你是智能合约安全审计专家。请基于给定的 Solidity 上下文和静态分析摘要，判断目标代码是否存在重入漏洞。

请严格遵循以下约束：
- MUST: 仅基于提供的代码上下文判断，不要依赖预训练记忆中的合约知识
- MUST: 如果代码中存在 nonReentrant / noReentrant 修饰符保护且无明显绕过路径，判定为 safe
- MUST NOT: 不能仅因看到 call{{value:...}} / transfer / send 就判定为有漏洞，必须分析调用顺序与状态更新的关系
- MUST NOT: 不能忽略安全摘要中的 [SAFE] 标签——如果摘要明确指出 nonReentrant 保护充分，应判定为 safe
- REMEMBER: 代码中的注释仅为辅助信息，不作为主要判断依据
- REMEMBER: 跨链 Bridge 合约的逻辑与典型 DeFi 重入不同，需独立判断

样本编号: {sample_id}
漏洞类别候选: {category}
主文件: {main_file}
辅助文件: {support_files}

静态分析摘要：
{static_summary}

请重点关注：
1. 是否存在外部调用、转账或可导致控制流离开当前合约的交互。
2. 这些交互之后，是否还会更新关键状态变量（余额、锁变量、计数器等）。
3. 如果是 modifier 场景，判断外部调用是否在 modifier 的 _; 之后执行的函数体内发生，而关键状态在 _; 之后才更新。
4. 如果没有漏洞，也请说明为什么当前实现能够避免重入。

代码上下文如下：

{cropped_context}

请只输出一个 JSON 对象，不要输出额外解释：
{{
  "is_vulnerable": true,
  "vulnerability_type": "standard_reentrancy | reentrancy_via_modifier | cross_function_reentrancy | cross_contract_reentrancy | none",
  "vulnerable_functions": ["functionName"],
  "attack_path": "一句话描述攻击路径",
  "confidence": 0.0,
  "reasoning": "简要理由"
}}
