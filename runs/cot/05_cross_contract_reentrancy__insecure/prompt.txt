你是顶尖 Web3 安全审计专家。请分析目标 Solidity 代码是否存在重入漏洞，并严格按审计步骤推理。

样本编号: 05_cross_contract_reentrancy__insecure
漏洞类别候选: cross_contract_reentrancy
主文件: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\05_cross_contract_reentrancy\InsecureMoonVault.sol
辅助文件: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\05_cross_contract_reentrancy\Dependencies.sol

静态分析摘要：
主合约文件: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\05_cross_contract_reentrancy\InsecureMoonVault.sol
辅助文件数量: 0
识别到的风险符号数量: 3
符号=InsecureMoonVault.deposit | 类型=function | 原因=external_calls | ETH发送=False | 外部调用=moonToken.mint( | 写状态=moonToken, success | 交互后更新状态=False
符号=InsecureMoonVault.withdrawAll | 类型=function | 原因=can_send_eth,external_calls | ETH发送=True | 外部调用=.calls*{, moonToken.burnAccount( | 写状态=moonToken, balance, success | 交互后更新状态=True
符号=InsecureMoonVault.getUserBalance | 类型=function | 原因=external_calls | ETH发送=False | 外部调用=moonToken.balanceOf( | 写状态=moonToken | 交互后更新状态=False

风险相关代码裁剪结果：
// Main contract: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\05_cross_contract_reentrancy\InsecureMoonVault.sol

contract InsecureMoonVault is ReentrancyGuard {
    IMoonToken public immutable moonToken;

// Risk-focused slice: deposit

function deposit() external payable noReentrant {  // Apply the noReentrant modifier
        bool success = moonToken.mint(msg.sender, msg.value);
        require(success, "Failed to mint token");
    }

// Risk-focused slice: withdrawAll

function withdrawAll() external noReentrant {  // Apply the noReentrant modifier
        uint256 balance = getUserBalance(msg.sender);
        require(balance > 0, "Insufficient balance");

        (bool success, ) = msg.sender.call{value: balance}("");
        require(success, "Failed to send Ether");

        success = moonToken.burnAccount(msg.sender);
        require(success, "Failed to burn token");
    }

// Risk-focused slice: getUserBalance

function getUserBalance(address _user) public view returns (uint256) {
        return moonToken.balanceOf(_user);
    }

请按以下步骤完成分析：
步骤 1：找出所有包含外部调用、ETH 转账或潜在控制流转移的函数/修饰器。
步骤 2：检查在外部交互之后，是否仍存在关键状态变量更新，尤其是余额、领取标记、锁变量、代币余额等。
步骤 3：如果涉及 modifier，请分析 modifier 中的外部调用是否会在函数主体执行前把控制权交给攻击者。
步骤 4：如果涉及多个函数，请分析攻击者是否可在回调中进入同合约的其他函数，造成跨函数重入。
步骤 5：给出最终结论，并只保留与重入判断直接相关的信息。

请只输出一个 JSON 对象，不要输出额外解释：
{
  "is_vulnerable": true,
  "vulnerability_type": "standard_reentrancy | reentrancy_via_modifier | cross_function_reentrancy | cross_contract_reentrancy | none",
  "vulnerable_functions": ["functionName"],
  "attack_path": "清晰描述攻击者如何重入",
  "confidence": 0.0,
  "reasoning": "按步骤浓缩后的最终理由"
}
