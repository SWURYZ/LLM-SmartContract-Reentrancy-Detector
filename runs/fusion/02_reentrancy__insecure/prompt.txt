你是智能合约系统级审计专家。请基于裁剪后的多文件上下文和静态分析摘要，按固定步骤判断主合约是否存在跨函数或跨合约重入风险。先在心中完成分析，再只输出最终 JSON，不要写出推理过程。

样本编号: 02_reentrancy__sample
漏洞类别候选: standard_reentrancy
主文件: SampleEtherVault.sol
辅助文件: 无

静态分析摘要：
主合约文件: SampleEtherVault.sol
辅助文件数量: 0
识别到的风险符号数量: 1
符号=SampleEtherVault.withdrawAll | 类型=function | 原因=can_send_eth | ETH发送=True | 外部调用=.calls*{ | 写状态=userBalances, balance | 交互后更新状态=True

多文件上下文：
// Main contract source

contract SampleEtherVault {
    mapping (address => uint256) private userBalances;

// Risk-focused slice: withdrawAll

function withdrawAll() external {
        uint256 balance = getUserBalance(msg.sender);
        require(balance > 0, "Insufficient balance");

        (bool success, ) = msg.sender.call{value: balance}("");
        require(success, "Failed to send Ether");

        userBalances[msg.sender] = 0;
    }

请按以下步骤完成分析：
步骤 1：找出所有可能把控制权交给外部地址或外部合约的函数、修饰器和调用点，尤其关注 transfer/call/send、外部函数调用和回调入口。
步骤 2：检查这些外部交互之后，是否仍然存在关键状态变量更新，重点是余额、锁变量、领取标记、计数器或权限状态。
步骤 3：如果是 modifier 场景，判断外部调用是否在函数主体执行前就把控制权交给攻击者。
步骤 4：如果同一合约里还有其他可被回调触发的函数，分析是否可能形成跨函数重入。
步骤 5：结合依赖合约或接口，判断回调是否可能进入不同合约路径，从而形成跨合约重入。
步骤 6：如果输入信息不足，优先给出 none，而不是凭空猜测。
步骤 7：最终结论只保留与重入判断直接相关的信息。

请只输出一个 JSON 对象，不要输出额外解释：
{
  "is_vulnerable": true,
  "vulnerability_type": "standard_reentrancy | reentrancy_via_modifier | cross_function_reentrancy | cross_contract_reentrancy | none",
  "vulnerable_functions": ["functionName"],
  "attack_path": "清晰描述主合约与相关合约之间的调用链",
  "confidence": 0.0,
  "reasoning": "按步骤浓缩后的最终理由"
}