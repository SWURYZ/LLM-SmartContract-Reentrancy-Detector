你是智能合约审计专家。请根据给定 Solidity 上下文，判断目标代码是否存在重入漏洞。

样本编号: 02_reentrancy__fixed
漏洞类别候选: standard_reentrancy
主文件: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\02_reentrancy\FixedEtherVault.sol
辅助文件: 无

请重点关注：
1. 是否存在外部调用、转账或可导致控制流离开当前合约的交互。
2. 这些交互之后，是否还会更新关键状态变量。
3. 如果没有漏洞，也请说明为什么当前实现能够避免重入。

代码上下文如下：

// Main contract: D:\Desktop\code\uestc\LLM-SmartContract-Reentrancy-Detector\contracts\02_reentrancy\FixedEtherVault.sol

contract FixedEtherVault is ReentrancyGuard {
    mapping (address => uint256) private userBalances;

// Risk-focused slice: withdrawAll

function withdrawAll() external noReentrant {  // FIX: Apply mutex lock
        uint256 balance = getUserBalance(msg.sender);
        require(balance > 0, "Insufficient balance");  // Check

        // FIX: Apply checks-effects-interactions pattern
        userBalances[msg.sender] = 0;  // Effect

        (bool success, ) = msg.sender.call{value: balance}("");  // Interaction
        require(success, "Failed to send Ether");
    }

请只输出一个 JSON 对象，不要输出额外解释：
{
  "is_vulnerable": true,
  "vulnerability_type": "standard_reentrancy | reentrancy_via_modifier | cross_function_reentrancy | cross_contract_reentrancy | none",
  "vulnerable_functions": ["functionName"],
  "attack_path": "一句话描述攻击路径",
  "confidence": 0.0,
  "reasoning": "简要理由"
}
